""" Configuration for the project """

from django.apps import AppConfig


class ProjectConfig(AppConfig):
    """Configuration for the project folder"""

    default_auto_field = "django.db.models.BigAutoField"
    name = "project"
